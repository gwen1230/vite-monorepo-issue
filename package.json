{
  "name": "monorepo",
  "version": "1.0.0",
  "description": "monorepo",
  "private": true,
  "scripts": {
    "build": "yarn workspaces foreach -Avvp --topological-dev run build"
  },
  "engines": {
    "node": ">=20.5.0"
  },
  "workspaces": [
    "packages/clients",
    "packages/dto"
  ],
  "devDependencies": {
    "typescript": "^5.3.3"
  },
  "packageManager": "yarn@4.1.0"
}
